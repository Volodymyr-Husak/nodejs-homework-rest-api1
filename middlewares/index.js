const { auth } = require("./auth");
const { userSubscriptionStandard } = require("./userSubscriptionStandard");
const { uploadAvatar } = require("./uploadAvatar");

module.exports = {
  auth,
  userSubscriptionStandard,
  uploadAvatar,
};
